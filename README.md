# 🧙‍♂️ Ask the Almighty Oracle

A playful Flask web app where users can ask life’s most important questions —
and receive answers that are **confidently wrong**.

This project demonstrates:
- Flask fundamentals (GET & POST)
- HTML forms
- OpenAI API integration
- Environment variable handling
- Basic error handling
- Simple, styled frontend

---

## ✨ Features

- Ask the Oracle any question
- The Oracle always answers incorrectly — but with confidence and style
- Mystical, humorous UI
- Clean Flask project structure

---

## 🛠️ Tech Stack

- Python 3
- Flask
- OpenAI API
- HTML / CSS
- python-dotenv

---

## 🚀 How to Run the Project

### 1. Clone the repository

```bash
git clone <your-repo-url>

cd <your-project-folder>
```
### 2. Create a virtual environment (optional but recommended)

```bash
python -m venv .venv

source .venv/bin/activate  # macOS / Linux
```

### 3. Install dependencies

```bash
pip install flask openai python-dotenv
```

### 4. Create a .env file

```bash
OPENAI_API_KEY=your_openai_api_key_here
```

⚠️ The .env file is intentionally not tracked by Git.

### 5. Run the app
```bash
python app.py
```

Open your browser at:

```bash
http://127.0.0.1:5000
```

# 🌀 Disclaimer

The Oracle is not meant to provide factual answers.
All responses are intentionally incorrect and purely for entertainment.

✨ So it is written.